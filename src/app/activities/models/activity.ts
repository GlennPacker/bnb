import { ActivityType } from './activityType';

export default class Activity {
  alt: string;
  activityType: ActivityType;
  icon: string;
  id: number;
  src: string;
  title: string;
  paras: string[];
}